{
  "name": "@optimai/cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for Optimai - Personal control center",
  "type": "module",
  "bin": {
    "optimai": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "express": "^4.21.2",
    "ical.js": "^2.1.0",
    "open": "^10.1.0",
    "ora": "^8.1.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.7",
    "tsup": "^8.3.6",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
